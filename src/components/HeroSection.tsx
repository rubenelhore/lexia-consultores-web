import React from 'react';

const HeroSection: React.FC = () => {
  return (
    <section className="hero">
      {/* Hero section content will go here */}
      <h2>Transformando la práctica legal en México con IA</h2>
      <p>Optimiza tu despacho, reduce tiempos y mejora la precisión con nuestra plataforma inteligente.</p>
      <button>Solicita una Demo</button>
    </section>
  );
};

export default HeroSection; 